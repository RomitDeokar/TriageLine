# agent runtime (all optional — the agent degrades gracefully without them)
faster-whisper>=1.0
onnxruntime>=1.17
huggingface_hub
tokenizers
numpy
pillow
# system: tesseract-ocr (optional, printed-label OCR for visual grounding)
