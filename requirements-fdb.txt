# Pinned dependencies for the official FDB-v3 reproduction (run_fdb_v3.sh, stage 1).
# Target interpreter: Python 3.11 or 3.12 (FDB-v3 README uses 3.10; livekit-agents 1.8.3 needs >=3.10).
# Versions below are the ones this repo was tested with (2026-09-26). Change them only together.

# --- LiveKit agent runtime (livekit_agent/cascaded_agent.py) ---
livekit-agents[openai]==1.8.3
livekit-plugins-openai==1.8.3
livekit-plugins-silero==1.8.3
livekit-plugins-deepgram==1.8.3      # optional free-credit STT/TTS provider (TRIAGELINE_STT/TTS_PROVIDER=deepgram)
livekit==1.1.18
livekit-api==1.2.1
python-dotenv==1.2.3
numpy==2.2.6
aiohttp==3.14.1

# --- official FDB-v3 runner + evaluators (v3/run_tool_benchmark*.py, evaluate_*.py) ---
openai==2.54.0                       # LLM judge client (gpt-4o, or any OpenAI-compatible endpoint via OPENAI_BASE_URL)
pydub==0.25.1
ffmpeg-python==0.2.0
gdown==6.4.0                         # official data release lives on Google Drive
# NeMo ASR (nvidia/parakeet-tdt-0.6b-v2) is used by the official runner to transcribe the agent's audio.
# It is large (torch). Installed separately by run_fdb_v3.sh unless --skip-nemo is given.
# nemo_toolkit[asr]==2.5.3
